/**
 * Created by zj on 2018/1/18.
 */
const config = {
    // 启动端口
    port: 3000,

    // 数据库配置
    database: {
        DATABASE: 'nodesql',
        USERNAME: 'root',
        PASSWORD: 'admin',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config